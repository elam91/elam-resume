const Intro = () => {
  return (
    <div className="w-full h-full bg-white  bg-clip-padding backdrop-filter backdrop-blur-lg bg-opacity-10 border border-gray-500 min-h-full rounded-xl">
      <div dangerouslySetInnerHTML={{ __html: "" }}></div>
    </div>
  );
};

export default Intro;
